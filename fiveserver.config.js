// Configuración para Five Server - archivo en la RAÍZ del proyecto
// Necesario solo si sirves archivos PHP con Five Server (puerto 5500).
// Asegúrate de que la ruta apunte al ejecutable de PHP correcto.

module.exports = {
  // Windows (ejemplo XAMPP)
  php: "C:\\xampp\\php\\php.exe"
  // macOS / Linux (ejemplo)
  // php: "/usr/bin/php"
};
